"""Autonomous work beyond the twenty: questions the benchmark does not ask.

Output lands in ``exploration/`` and never in ``cases/``. The distinction is
load-bearing — ``cases/`` is the graded submission, and a speculative finding
filed there would be a wrong answer rather than an extra one.
"""
